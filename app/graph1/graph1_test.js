'use strict';

describe('kineticGraphs.graph1 module', function() {

  beforeEach(module('kineticGraphs.graph1'));

  describe('graph1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var graph1Ctrl = $controller('Graph1Ctrl');
      expect(graph1Ctrl).toBeDefined();
    }));

  });
});