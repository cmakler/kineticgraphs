'use strict';

describe('kineticGraphs.graph2 module', function() {

  beforeEach(module('kineticGraphs.graph2'));

  describe('graph2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var graph2Ctrl = $controller('Graph2Ctrl');
      expect(graph2Ctrl).toBeDefined();
    }));

  });
});